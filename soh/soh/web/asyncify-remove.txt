# Functions Asyncify must not instrument. ZAPD and OTRExporter run on the extraction
# thread and never suspend; instrumenting them only bloats the wasm and can overflow
# the worker stack in Safari.
Z*::*
*Exporter::*
Declaration::*
GameConfig::*
Globals::*
ImageBackend::*
OutputFormatter::*
WarningHandler::*
tinyxml2::*
# The randomizer's table builders run once at startup and never suspend. They are
# compiled at -O0 into huge functions that Asyncify is very slow to instrument.
Rando::StaticData::HintTable_Init*
Rando::StaticData::InitItemTable*
Rando::StaticData::InitLocationTable*
